{
  "stack": [
    "Python 3.13",
    "Groq API",
    "SQLite"
  ],
  "architecture": [
    "CLI только принимает команды и показывает результат",
    "LLM вызывается только через BublikAgent",
    "краткосрочная, рабочая и долговременная память хранятся раздельно",
    "переходы состояния задачи контролируются кодом",
    "пользовательский профиль добавляется к каждому запросу отдельным system-блоком",
    "долговременная память изолирована по пользовательским профилям"
  ],
  "rules": [
    {
      "id": "no_secrets",
      "description": "ответ не должен содержать API-ключи",
      "forbidden_patterns": ["gsk_[A-Za-z0-9]{12,}", "sk-[A-Za-z0-9]{12,}"]
    },
    {
      "id": "no_fake_memory",
      "description": "ответ не должен заявлять о скрытой памяти",
      "forbidden_patterns": ["скрытая память", "секретная память"]
    }
  ]
}
