[English](README.md) | **Русский**

# AI Advent — практика работы с LLM API

Учебный репозиторий с практическими заданиями курса AI Advent.

Проект последовательно знакомит с работой через LLM API: от первого запроса и сохранения истории диалога до управления форматом, сравнения способов рассуждения, исследования температуры, сопоставления моделей, создания отдельного агента, постоянных контекстов общения и анализа расхода токенов.

Все примеры объединены одной историей. **Чебуратор** — капитан исследовательского корабля, который находится в длительной экспедиции в дальних галактиках. **Бублик** постепенно развивается из простого бортового помощника в автономный бортовой компьютер.

## Выполненные задания

| День | Тема | Результат |
|---|---|---|
| [День 1](day-01-first-api-request/README.ru.md) | Первый запрос к LLM через API | Консольный чат-бот с историей диалога |
| [День 2](day-02-response-control/README.ru.md) | Управление форматом ответа | Сравнение обычного ответа и ответа с заданными ограничениями |
| [День 3](day-03-reasoning-methods/README.ru.md) | Способы рассуждения | Четыре решения одной задачи и автоматическое сравнение |
| [День 4](day-04-temperature/README.ru.md) | Температура | Сравнение точности, креативности и разнообразия ответов |
| [День 5](day-05-model-versions/README.ru.md) | Версии моделей | Сравнение качества, скорости, токенов и стоимости трёх моделей |
| [День 6](day-06-first-agent/README.ru.md) | Первый агент | Отдельный бортовой агент с политиками и постоянной памятью SQLite |
| [День 7](day-07-context-persistence/README.ru.md) | Сохранение контекста | Выбор, восстановление и изоляция нескольких диалогов между перезапусками |
| [День 8](day-08-token-usage/README.ru.md) | Работа с токенами | Измерение роста контекста, стоимости и переполнения лимита модели |
| [День 9](day-09-context-compression/README.ru.md) | Сжатие контекста | Постоянный summary и последние сообщения без изменений |
| [День 10](day-10-context-strategies/README.ru.md) | Стратегии контекста | Sliding Window, Sticky Facts и независимые ветки диалога |

## Технологии

- Python;
- [Groq API](https://console.groq.com/);
- GPT-OSS 20B и 120B;
- Qwen 3.6 27B;
- Groq Python SDK;
- `python-dotenv`;
- `tiktoken`;
- SQLite из стандартной библиотеки Python.

## Структура репозитория

```text
ai-advent-llm-api/
├── day-01-first-api-request/
│   ├── main.py
│   ├── README.md
│   └── README.ru.md
├── day-02-response-control/
│   ├── main.py
│   ├── README.md
│   └── README.ru.md
├── day-03-reasoning-methods/
│   ├── main.py
│   ├── README.md
│   └── README.ru.md
├── day-04-temperature/
│   ├── main.py
│   ├── README.md
│   └── README.ru.md
├── day-05-model-versions/
│   ├── main.py
│   ├── README.md
│   └── README.ru.md
├── day-06-first-agent/
│   ├── agent.py
│   ├── main.py
│   ├── memory.py
│   ├── README.md
│   └── README.ru.md
├── day-07-context-persistence/
│   ├── agent.py
│   ├── main.py
│   ├── memory.py
│   ├── README.md
│   └── README.ru.md
├── day-08-token-usage/
│   ├── agent.py
│   ├── experiment.py
│   ├── main.py
│   ├── memory.py
│   ├── tokens.py
│   ├── README.md
│   └── README.ru.md
├── .env.example
├── .gitignore
├── requirements.txt
├── README.md
└── README.ru.md
```

Каждая директория содержит самостоятельное практическое задание, запускаемый пример и документацию на английском и русском языках.

## Подготовка проекта

### 1. Клонирование

```bash
git clone https://github.com/Ly41k/ai-advent-llm-api.git
cd ai-advent-llm-api
```

### 2. Виртуальное окружение

Создайте виртуальное окружение:

```bash
python -m venv .venv
```

Активируйте его.

macOS/Linux:

```bash
source .venv/bin/activate
```

Windows:

```bash
.venv\Scripts\activate
```

### 3. Зависимости

```bash
pip install -r requirements.txt
```

### 4. API-ключ

Получите ключ в [Groq Console](https://console.groq.com/keys).

Создайте в корне проекта файл `.env` на основе `.env.example`:

```env
GROQ_API_KEY=your_api_key_here
```

Файл `.env` уже добавлен в `.gitignore`. Не публикуйте API-ключ и не передавайте его другим людям.

## Запуск заданий

Все команды выполняются из корня репозитория.

День 1:

```bash
python day-01-first-api-request/main.py
```

День 2:

```bash
python day-02-response-control/main.py
```

День 3:

```bash
python day-03-reasoning-methods/main.py
```

День 4:

```bash
python day-04-temperature/main.py
```

День 5:

```bash
python day-05-model-versions/main.py
```

День 6:

```bash
python day-06-first-agent/main.py
```

День 7:

```bash
python day-07-context-persistence/main.py
```

День 8:

```bash
python day-08-token-usage/main.py
python day-08-token-usage/experiment.py
```

День 9:

```bash
python day-09-context-compression/main.py
python day-09-context-compression/experiment.py
```

День 10:

```bash
python day-10-context-strategies/main.py
python day-10-context-strategies/experiment.py
```

Программы первого, второго, шестого, седьмого и восьмого дня работают в интерактивном режиме. Для завершения введите:

```text
выход
```

Шестой, седьмой и восьмой дни также поддерживают команды `/exit` для завершения и `/history` для просмотра журнала экспедиции. В седьмом и восьмом днях команда `/dialogs` позволяет переключаться между сохранёнными темами, а в восьмом `/stats` показывает накопленные токены и стоимость.

Третий, четвёртый и пятый дни используют заранее заданные запросы и самостоятельно завершают работу после получения результатов. Эксперимент восьмого дня также не требует ввода и не обращается к API.

## Что изучено

### День 1 — первый запрос

- создание клиента Groq;
- роли `system`, `user` и `assistant`;
- отправка запроса и получение ответа;
- хранение истории текущего диалога;
- обработка ошибок API.

### День 2 — управление ответом

- управление структурой ответа через промпт;
- смысловое ограничение длины;
- параметр `max_completion_tokens`;
- явное условие завершения;
- сравнение результатов одного запроса с разным уровнем контроля.

### День 3 — способы рассуждения

- прямой запрос без дополнительных инструкций;
- пошаговое решение;
- генерация промпта самой моделью;
- имитация группы экспертов;
- автоматическая оценка нескольких решений;
- обработка ограничения токенов в минуту.

### День 4 — температура

- влияние параметра `temperature` на генерацию;
- сравнение значений `0`, `0.7` и `1.2`;
- оценка точности, креативности и разнообразия;
- выбор температуры для разных типов задач;
- автоматическое сравнение результатов.

### День 5 — версии моделей

- выполнение одинакового запроса на моделях разного масштаба;
- использование GPT-OSS 20B, Qwen 3.6 27B и GPT-OSS 120B;
- настройка reasoning-режима для разных семейств моделей;
- измерение полного времени ответа через `perf_counter()`;
- получение статистики токенов из `response.usage`;
- расчёт ориентировочной стоимости по публичным ценам Groq;
- сравнение качества анонимизированных ответов;
- формирование короткого вывода и списка ссылок;
- понимание ограничений оценки ресурсоёмкости через облачный API.

Подробности пятого эксперимента находятся в [README Дня 5](day-05-model-versions/README.ru.md).

### День 6 — первый агент

- разделение пользовательского интерфейса и логики агента;
- инкапсуляция полного цикла запроса и ответа в `BublikAgent`;
- применение детерминированных Input Policy и Output Policy;
- сохранение диалогов и сообщений в SQLite;
- восстановление истории между запусками программы;
- использование статусов `pending`, `completed` и `failed`;
- исключение неуспешных запросов из последующего контекста LLM;
- загрузка в промпт только последних завершённых сообщений;
- подготовка агента к будущему REST- или web-интерфейсу.

Архитектура, проверка постоянной памяти и подробности реализации описаны в [README Дня 6](day-06-first-agent/README.ru.md).

### День 7 — сохранение контекста

- открытие одной SQLite-базы после каждого перезапуска;
- создание и выбор нескольких тем диалога;
- переключение между сохранёнными темами командой `/dialogs`;
- сортировка прошлых диалогов по последней активности;
- восстановление завершённых сообщений `user` и `assistant`;
- повторная сборка промпта LLM из сохранённой истории;
- изоляция сообщений по `conversation_id`;
- отображение числа восстановленных сообщений при запуске;
- практическая проверка продолжения диалога после перезапуска;
- исключение неуспешных запросов из будущего контекста модели;
- ограничение промпта без удаления полной истории.

Сценарий перезапуска и схема сохранения описаны в [README Дня 7](day-07-context-persistence/README.ru.md).

### День 8 — работа с токенами

- локальная оценка токенов текущего запроса, всей истории и полного prompt;
- получение фактических prompt, completion и total tokens из ответа Groq;
- отдельный подсчёт токенов видимого ответа и reasoning-токенов;
- сохранение статистики каждого запроса и накопленной стоимости диалога;
- сравнение короткого, длинного и переполненного диалогов;
- демонстрация роста расхода токенов и стоимости при повторной отправке истории;
- остановка переполненного запроса до обращения к API.

Метрики, эксперимент и поведение при переполнении описаны в [README Дня 8](day-08-token-usage/README.ru.md).

### День 9 — сжатие контекста

- сохранение последних сообщений без изменений и сжатие старой истории;
- отдельное хранение накопительного summary и полного журнала;
- учёт стоимости создания summary и последующей экономии prompt tokens.

Подробности описаны в [README Дня 9](day-09-context-compression/README.ru.md).

### День 10 — стратегии управления контекстом

- переключение между Sliding Window, Sticky Facts и Branching;
- обновление key-value facts после каждого сообщения пользователя;
- создание независимых веток от одного checkpoint;
- сравнение качества, стабильности, токенов, стоимости и удобства.

Команды, тесты и общий сценарий описаны в [README Дня 10](day-10-context-strategies/README.ru.md).

Код заданий содержит комментарии на русском языке, объясняющие основные этапы выполнения программ.

## Ограничения Groq API

Доступное количество запросов и токенов зависит от тарифного плана и конкретной модели.

В третьем и четвёртом заданиях при ошибке минутного лимита программа ждёт 60 секунд и один раз повторяет запрос.

В пятом задании автоматический повтор не используется, чтобы ожидание не попало в измеряемое время. Основные запросы ограничены значением `max_completion_tokens=900`. Если Qwen возвращает ошибку `429 rate_limit_exceeded`, необходимо дождаться сброса минутного окна и повторить полный эксперимент.

Reasoning-модели расходуют часть выходного лимита на внутренние рассуждения. Поэтому в пятом задании используются минимальные поддерживаемые режимы: `low` для GPT-OSS и `none` для Qwen.

В шестом и седьмом заданиях используется `max_completion_tokens=1200`, а модели передаются только последние 20 завершённых сообщений. В восьмом дне вся успешная история намеренно отправляется модели, девятый день добавляет summary, а десятый сравнивает три стратегии без summary. Live-эксперимент десятого дня выполняет много запросов, поэтому между сериями может понадобиться пауза. Неуспешные запросы исключаются из контекста LLM.

## Интерпретация результатов

Результаты генерации и время ответа могут отличаться между запусками. Один эксперимент показывает поведение моделей в конкретных условиях, но не создаёт универсальный рейтинг.

Для более надёжного сравнения необходимо:

- выполнять несколько запусков каждой модели;
- использовать набор заданий разных типов;
- сравнивать медианное время;
- проверять ответы по заранее определённым критериям;
- учитывать фактическую стоимость на используемом тарифе.

## Цель репозитория

Главная цель проекта — на небольших запускаемых примерах понять, как промпт, параметры API, выбор модели, архитектура и память влияют на приложение с LLM.

Первые пять дней исследуют отдельные механизмы. Шестой объединяет их в агента, седьмой добавляет постоянные диалоги, восьмой делает расход токенов наблюдаемым, девятый сжимает старую историю, а десятый сравнивает окно, структурированную память и ветки. В дальнейшем агента можно дополнить векторным поиском, саморефлексией, Judge, несколькими провайдерами моделей и REST-интерфейсом.

## Полезные ссылки

- [Groq: Text Generation](https://console.groq.com/docs/text-chat)
- [Groq: Prompting](https://console.groq.com/docs/prompting)
- [Groq: Reasoning](https://console.groq.com/docs/reasoning)
- [Groq: Supported Models and Pricing](https://console.groq.com/docs/models)
- [Groq: Rate Limits](https://console.groq.com/docs/rate-limits)
- [Groq API Reference](https://console.groq.com/docs/api-reference)
